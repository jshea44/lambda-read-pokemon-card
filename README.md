# Lambda Read Pokemon Card

A lambda function that reads all pokemon in DynamoDB.

## Feature Tasks

- Routing: API Gateway

  - GET
    - /pokemon - returns an array of objects representing the records in the database.

## Testing

- Should return a list of pokemon
